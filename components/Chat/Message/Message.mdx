import { Meta } from '@storybook/addon-docs/blocks';

<Meta title='Markdown Rendering/Message Rendering' />

# Message Syntax

## Markdown Rendering

Message rendering supports [normal markdown syntax](https://www.markdownguide.org/basic-syntax/) via the [react-markdown package](https://www.npmjs.com/package/react-markdown).

It also supports custom code block rendering. 