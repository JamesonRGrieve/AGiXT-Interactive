services:
  agixt:
    image: joshxt/agixt:latest
    init: true
    environment:
      DATABASE_TYPE: ${DATABASE_TYPE:-sqlite}
      DATABASE_NAME: ${DATABASE_NAME:-models/agixt}
      UVICORN_WORKERS: ${UVICORN_WORKERS:-10}
      AGINTERACTIVE_API_KEY: ${AGINTERACTIVE_API_KEY:-None}
      AGINTERACTIVE_URI: ${AGINTERACTIVE_URI:-http://agixt:7437}
      MAGIC_LINK_URL: ${AUTH_WEB:-http://localhost:1109}
      DISABLED_EXTENSIONS: ${DISABLED_EXTENSIONS}
      DISABLED_PROVIDERS: ${DISABLED_PROVIDERS}
      WORKING_DIRECTORY: ${WORKING_DIRECTORY:-/agixt/WORKSPACE}
      REGISTRATION_DISABLED: ${REGISTRATION_DISABLED:-false}
      TOKENIZERS_PARALLELISM: 'false'
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      AWS_CLIENT_ID: ${AWS_CLIENT_ID}
      AWS_CLIENT_SECRET: ${AWS_CLIENT_SECRET}
      AWS_REGION: ${AWS_REGION}
      AWS_USER_POOL_ID: ${AWS_USER_POOL_ID}
      GITHUB_CLIENT_ID: ${GITHUB_CLIENT_ID}
      GITHUB_CLIENT_SECRET: ${GITHUB_CLIENT_SECRET}
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID}
      GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET}
      MICROSOFT_CLIENT_ID: ${MICROSOFT_CLIENT_ID}
      MICROSOFT_CLIENT_SECRET: ${MICROSOFT_CLIENT_SECRET}
      STORAGE_BACKEND: ${STORAGE_BACKEND:-local}
      STORAGE_CONTAINER: ${STORAGE_CONTAINER:-aginteractive-workspace}
      B2_KEY_ID: ${B2_KEY_ID:-}
      B2_APPLICATION_KEY: ${B2_APPLICATION_KEY:-}
      B2_REGION: ${B2_REGION:-us-west-002}
      S3_BUCKET: ${S3_BUCKET:-aginteractive-workspace}
      S3_ENDPOINT: ${S3_ENDPOINT:-http://minio:9000}
      AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID:-minioadmin}
      AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY:-minioadmin}
      AWS_STORAGE_REGION: ${AWS_STORAGE_REGION:-us-east-1}
      AZURE_STORAGE_ACCOUNT_NAME: ${AZURE_STORAGE_ACCOUNT_NAME:-}
      AZURE_STORAGE_KEY: ${AZURE_STORAGE_KEY:-}
      SEED_DATA: ${SEED_DATA:-true}
      AGENT_NAME: ${AGINTERACTIVE_AGENT:-XT}
      AGENT_PERSONA: ${AGENT_PERSONA}
      TRAINING_URLS: ${TRAINING_URLS}
      ENABLED_COMMANDS: ${ENABLED_COMMANDS}
      EZLOCALAI_VOICE: ${EZLOCALAI_VOICE}
      ANTHROPIC_MODEL: ${ANTHROPIC_MODEL}
      DEEPSEEK_MODEL: ${DEEPSEEK_MODEL}
      AZURE_MODEL: ${AZURE_MODEL}
      GOOGLE_MODEL: ${GOOGLE_MODEL}
      OPENAI_MODEL: ${OPENAI_MODEL}
      XAI_MODEL: ${XAI_MODEL}
      EZLOCALAI_MAX_TOKENS: ${EZLOCALAI_MAX_TOKENS}
      DEEPSEEK_MAX_TOKENS: ${DEEPSEEK_MAX_TOKENS}
      AZURE_MAX_TOKENS: ${AZURE_MAX_TOKENS}
      XAI_MAX_TOKENS: ${XAI_MAX_TOKENS}
      OPENAI_MAX_TOKENS: ${OPENAI_MAX_TOKENS}
      ANTHROPIC_MAX_TOKENS: ${ANTHROPIC_MAX_TOKENS}
      GOOGLE_MAX_TOKENS: ${GOOGLE_MAX_TOKENS}
      AZURE_API_KEY: ${AZURE_API_KEY}
      GOOGLE_API_KEY: ${GOOGLE_API_KEY}
      OPENAI_API_KEY: ${OPENAI_API_KEY}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY}
      EZLOCALAI_API_KEY: ${EZLOCALAI_API_KEY}
      DEEPSEEK_API_KEY: ${DEEPSEEK_API_KEY}
      XAI_API_KEY: ${XAI_API_KEY}
      AZURE_OPENAI_ENDPOINT: ${AZURE_OPENAI_ENDPOINT}
      EZLOCALAI_URI: ${EZLOCALAI_URI}
      GRAPHIQL: ${GRAPHIQL:-true}
      TZ: ${TZ:-America/New_York}
    ports:
      - '${AGINTERACTIVE_PORT:-7437}:7437'
    volumes:
      - ./models:/agixt/models
      - ./WORKSPACE:/agixt/WORKSPACE
      - /var/run/docker.sock:/var/run/docker.sock
  aginteractive:
    image: jamesonrgrieve/aginteractive:main
    init: true
    build:
      context: .
      args:
        MODE: production
        NEXT_TELEMETRY_DISABLED: 1
        AGINTERACTIVE_AGENT: ${AGINTERACTIVE_AGENT:-XT}
        AGINTERACTIVE_FOOTER_MESSAGE: ${AGINTERACTIVE_FOOTER_MESSAGE:-JamesonRGrieve 2024}
        AGINTERACTIVE_SERVER: ${AGINTERACTIVE_SERVER:-http://localhost:7437}
        APP_DESCRIPTION: ${APP_DESCRIPTION-AGiXT is an advanced artificial intelligence agent orchestration agent.}
        APP_NAME: ${APP_NAME:-AGiXT}
        APP_URI: ${APP_URI:-http://localhost:1109}
        LOG_VERBOSITY_SERVER: ${LOG_VERBOSITY_SERVER:-3}
        AGINTERACTIVE_FILE_UPLOAD_ENABLED: ${AGINTERACTIVE_FILE_UPLOAD_ENABLED:-true}
        AGINTERACTIVE_VOICE_INPUT_ENABLED: ${AGINTERACTIVE_VOICE_INPUT_ENABLED:-true}
        AGINTERACTIVE_RLHF: ${AGINTERACTIVE_RLHF:-true}
        AGINTERACTIVE_ALLOW_MESSAGE_EDITING: ${AGINTERACTIVE_ALLOW_MESSAGE_EDITING:-true}
        AGINTERACTIVE_ALLOW_MESSAGE_DELETION: ${AGINTERACTIVE_ALLOW_MESSAGE_DELETION:-true}
        AGINTERACTIVE_SHOW_OVERRIDE_SWITCHES: ${AGINTERACTIVE_SHOW_OVERRIDE_SWITCHES:-tts,websearch,analyze-user-input}
        AGINTERACTIVE_CONVERSATION_MODE: ${AGINTERACTIVE_CONVERSATION_MODE:-select}
        INTERACTIVE_MODE: ${INTERACTIVE_MODE:-chat}
        ALLOW_EMAIL_SIGN_IN: ${ALLOW_EMAIL_SIGN_IN:-true}
        GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID}
        GITHUB_CLIENT_ID: ${GITHUB_CLIENT_ID}
        MICROSOFT_CLIENT_ID: ${MICROSOFT_CLIENT_ID}
        TZ: ${TZ:-America/New_York}
    ports:
      - '${AGINTERACTIVE_INTERACTIVE_PORT:-1109}:1109'
    restart: unless-stopped
    volumes:
      - ./node_modules:/app/node_modules
